<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Material 3。DayNight でライト/ダーク両対応。 -->
    <style name="Theme.Disproid" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="colorPrimary">@color/brand_primary</item>
        <item name="colorOnPrimary">@color/brand_on_primary</item>
        <item name="colorPrimaryContainer">@color/brand_primary_container</item>
        <item name="colorOnPrimaryContainer">@color/brand_on_primary_container</item>
    </style>

    <!-- 起動スプラッシュ（androidx core-splashscreen）。
         ブランド色背景 + 白い円 + アイコンで視認性を確保。 -->
    <style name="Theme.Disproid.Splash" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/brand_primary</item>
        <item name="windowSplashScreenIconBackgroundColor">@android:color/white</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/ic_launcher_foreground</item>
        <item name="postSplashScreenTheme">@style/Theme.Disproid</item>
    </style>

    <!-- ミラー表示用の全画面・黒背景テーマ -->
    <style name="Theme.Disproid.Mirror" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="android:windowBackground">@android:color/black</item>
        <item name="android:statusBarColor">@android:color/black</item>
        <item name="android:navigationBarColor">@android:color/black</item>
    </style>
</resources>
